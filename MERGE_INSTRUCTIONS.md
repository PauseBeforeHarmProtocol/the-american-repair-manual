# Merge into `PauseBeforeHarmProtocol/the-american-repair-manual`

1. Create `agent/arm-repository-currentization-2026-07-18` from `main`.
2. Copy this overlay into the repository root.
3. Preserve both existing HTML files.
4. Run `python scripts/prepare_release.py` and `python scripts/validate.py`.
5. Review the generated download, current-release JSON, and checksum.
6. Open a draft pull request; do not describe the infrastructure pass as an independent policy-content validation.
